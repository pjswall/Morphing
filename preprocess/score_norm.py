import os
import re
import pandas as pd
from deepface import DeepFace
from tqdm import tqdm
import scipy as sp 
import numpy as np

from sklearn.preprocessing import MinMaxScaler
from pandas import read_csv

def normalise(a, a_min, a_max):

	s = (a - a_min) / (a_max - a_min)
	return s

names = ['imgname','isMorph','gt1_gt2','gt1_input','gt2_input','output1_input','output2_input','gt1_output1','gt1_output2','gt2_output1','gt2_output2','output1_output2' ]
df = read_csv('scores.csv',names= names)

array = df.values

X = array[1:,2:]

for i in range(113):
	for j in range(10):
		a = float(X[i,j])
		a_min = float(np.min(X[i]))
		a_max = float(np.max(X[i]))
        
		X[i,j] = str(normalise(a,a_min,a_max))


print(X[:10])
print(X.shape)

df_norm = read_csv('scores.csv')

df_norm2 = pd.DataFrame(X)

df_norm2.columns = ['gt1_gt2', 'gt1_input',  'gt2_input',  'output1_input',  'output2_input',   'gt1_output1', 'gt1_output2', 'gt2_output1', 'gt2_output2', 'output1_output2']
df_norm2['imgname'] = df_norm['imgname']
df_norm2['isMorph'] = df_norm['isMorph']

print(df_norm2[-7:])

df_norm2.to_csv('new_norm.csv')










































































































































































































































































































































